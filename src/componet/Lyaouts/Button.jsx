import React from 'react'

const Button = ({title}) => {
  return (
    <button className='bg-primary text-white py-4 px-[85px] font-dm font-bold text-sm'>{title}</button>
  )
}

export default Button